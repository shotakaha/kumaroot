# ブラウズしたい（`TBrowser` / `RBrowser`）

```console
$ root
root [0] TBrowser b;
```

`TBrowser`はROOTのオブジェクトをブラウズするためのツールです。
ROOTファイルの中身を確認したり、ヒストグラムやグラフをダブルクリックして表示したりできます。

ROOT6以降では、`RBrowser`という新しいブラウザも利用可能です。
最新のROOTでは、`TBrowser`と入力すると`RBrowser`が起動します。
