# リモートリポジトリの設定（``git remote``）

後述の :command:`git clone` をすると、
リモートリポジトリは自動で設定されるので、
実はあんまり使う機会がないかもしれません。

そのため、使い方を忘れやすいので、ヘルプで確認するとよいと思います。

```bash
$ git remote -h
```

```bash
$ git remote -v
```
