\documentclass[slide]{ltjsarticle}
%\documentclass[landscape]{jlreq}

\usepackage[showframe]{geometry}
\geometry{
    margin=10truemm,
    headheight=20truemm,
    headsep=10truemm,}
\usepackage{layout}

\usepackage{mwe}

\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhead{}
\fancyhead[R]{\includegraphics[height=1em]{example-image.pdf}}
\fancyfoot{}
\fancyfoot[R]{{\small}\thepage}

\usepackage[deluxe,match,hiragino-pron]{luatexja-preset}
\setmonofont{MoralerspaceKrypton}
\setmonojfont{MoralerspaceKrypton}


\usepackage[hybrid]{markdown}
\usepackage{minted}

\usepackage{enumitem}

% 見出し（section）で改ページ
\usepackage[compact]{titlesec}
%\titlelabel{\thetitle.\quad}
% \titleformat{<command>}[<shape>]{<format>}{<label>}{<sep>}{<before-code>}[<after-code>]
% 1. {<command>} : 再定義する見出しコマンド（\part, \chapter, \section, \subsection, \subsubsection, \paragraph, \subparagraph）
% -. [<shape>]: paragraph shape（hang, block, display, runin, leftmargin, rightmargin, drop, wrap, frame)
% 2. {<format>}: format applied to whole title (= label and text).
% 3. {<label>}: define label.
% 4. {<sep>}: horizontal separation btw label and title body. 長さを設定する。displayの場合はvertical方向のアキ。frameの場合はタイトルと枠のパディング
% 5. {<before-code>}: code preceding the title body
% -. [<after-code>]: code following the title body
\titleformat{\section}{\newpage\ebseries\Large}{\ebseries\thesection.}{0.1em}{}
% \titlespacing{<command>}{<left>}{<before-sep>}{<after-sep>}{<right-sep>}
% \titleline[<align>]{<horizontal material}
%\titleline[l]{\rule{\textwidth}{1em}}


\title{日本語スライド作成}
\author{qumasan}

\begin{document}

\layout

\section{はじめに}

\begin{markdown}
- `ltjsarticle'でスライド
- それ
- あれ
\end{markdown}

\section{ドキュメントクラス}

\begin{markdown}
```latex
\documentclass[slide]{ltjsarticle}
```

- クラス: `ltjsarticle`
- オプション: `slide`

\end{markdown}

\section{パッケージ読み込み}

\begin{markdown}
```latex
\usepackage{geometry}
\usepackage[hybrid]{markdown}
\usepackage{enumitem}
```
\end{markdown}

\end{document}
