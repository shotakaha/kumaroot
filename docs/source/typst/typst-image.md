# 図したい（`#image`）

```typst
#image("image.jpg", width: 80%)
```

`#image`要素で画像を挿入できます。
第一引数に画像ファイルのパスを指定します。
画像ファイルは、ドキュメントと同じディレクトリに置くか、相対パスで指定します。

`width`オプションで画像の幅を指定できます。
幅は、絶対値（例: `100pt`）や相対値（例: `80%`）で指定できます。
`height`オプションで画像の高さを指定できます。
幅と高さの両方を指定すると画像が歪む可能性があるので、幅か高さのどちらか一方を指定することをオススメします。
