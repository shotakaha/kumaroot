# 全体設定したい（`zensical.toml`）

Zensicalの全体設定は、プロジェクトルートに配置した`zensical.toml`ファイルで行います。
このファイルは、Zensicalのビルドプロセス全体に影響を与える設定を含むことができます。

:::{note}

Zensicalは、MkDocs for Materialの設定をほぼそのまま引き継ぐことができます。
MkDocs for Materialで構築していたサイトは、まずは`mkdocs.yml`のままでも動かすことができます。

:::
