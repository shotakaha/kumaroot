# cd

```bash
$ cd ディレクトリ名
```

ディレクトリを移動するためのコマンドです。

## ホームディレクトリに移動したい

```bash
$ cd
```

引数なしで``cd``するとホームディレクトリに移動できます。

## 親ディレクトリに移動したい

```bash
$ cd ..
```

## 直前のディレクトリに移動したい

```bash
$ cd -
```
