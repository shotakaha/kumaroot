```{eval-rst}
.. index::
    single: CLI; ls
    single: List directory contents; ls
```

# ファイル情報したい（`ls`）

```console
$ ls
$ ls -a
$ ls -ltr
```

`ls`コマンドは、ディレクトリの中身を一覧表示するコマンドです。
もっともよく使われる基本コマンドのひとつです。

:::{seealso}

- [](./command-exa.md)
- [](./command-lsd.md)

:::

## 色付けしたい（`ls -G`）

```console
$ ls -G
```

`-G`で、ファイル種別ごとに色付けして表示できます。
最近のターミナルでは、デフォルトで色付けされることが多いので、あえてオプションを指定する必要はないかもしれません。

## ディレクトリ識別したい（`ls -F`）

```console
$ ls -F
```

`-F`で、ディレクトリの末尾に`/`をつけて表示できます。
また、実行可能ファイルには`*`、シンボリックリンクには`@`がつきます。
色なしの環境でもファイル種別が識別しやすくなるので、上書きエイリアスとして設定しておくことをオススメします。

```bash
alias ls='ls -F'
```

## 隠しファイルを表示したい（`ls -a`）

```console
$ ls -a
```

`-a`で、隠しファイルも含めたすべてのファイルを表示できます。
`.gitignore`や`.DS_Store`のように`.`ではじまるファイルは、ユーザー設定やシステム設定に関するファイルのため、通常の`ls`では表示されないようになっています。

## 最新ファイルを確認したい（`ls -ltr`）

```console
$ ls -ltr
$ ls -ltra
```

`-ltr`で、最近更新したファイルを確認できます。
`-t`は更新日時の新しい順に並べるオプション、
`-r`は逆順に並べるオプションです。
`-l`や`-a`は状況によって使い分けます。

一番新しいファイルが出力の末尾に表示されるので、
わざわざコマンドを実行した画面までスクロールする必要がなくなります。

## ファイルを1行ごとに表示したい（`ls -1`）

```bash
$ ls -1
```

`-1`で、ファイルを1行ごとに表示できます。
デフォルトの`ls`では、ターミナルの横幅に合わせて、複数列で表示されます。

`xargs`の引数にファイル名を渡したい場面で便利です。
