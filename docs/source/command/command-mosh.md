# SSH接続したい（`mosh`）

```console
$ mosh username@hostname
```

`mosh`は、リモートサーバーに安定して接続するためのコマンドです。
SSHの代替として使うことができます。

UDPを使って接続するため、ネットワークの切り替えや、接続が不安定な環境でも、接続が切れにくい特徴があります。

:::{caution}

`mosh`は、SSHを置き換えるものではありません。

:::
