# 絞り込み検索したい（`peco`）

```console
$ peco ファイル名
$ コマンド | peco
$ history | peco
$ ps aux | peco
$ git log --oneline | peco
```

`peco`は、絞り込み検索（incremental search）できるコマンドです。
ほとんどすべての入力に対して「絞り込み検索」で候補を絞り込むことができます。
大量のデータに対しても高速で動作します。
